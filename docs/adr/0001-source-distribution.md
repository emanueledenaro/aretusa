# Source-first distribution

Aretusa installs editable source files into the consumer project through its own CLI and registry. This preserves local ownership and supports original themes without a permanently opaque runtime dependency. Updates must show conflicts and never overwrite modified source silently.

