# Formisch

Issue: #90. Typed `FormischField` adapter over `useField` from `@formisch/react`, with Aretusa label, help text, error and focus anatomy and a `controlProps.ref` that registers the control for invalid-submit focus. Verify schema-typed fields, validation, error association, submission lifecycle with pending, success, failure and retry, reset with initial input and dynamic array fields, plus the responsive matrix on the documented example. All quality gates remain pending until evidence is recorded.

Aretusa-Item: formisch
