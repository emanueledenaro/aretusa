# React Hook Form

Issue: #88. Typed `HookFormField` adapter over `useController` with Aretusa label, help text, error and focus anatomy; the caller owns the control and the form. Verify text, select, checkbox, radio and switch mapping, synchronous and asynchronous validation, linked errors, focus on invalid submit, pending, success, failure and retry, reset with defaults and field arrays, plus the responsive matrix on the documented example. All quality gates remain pending until evidence is recorded.

Aretusa-Item: react-hook-form
