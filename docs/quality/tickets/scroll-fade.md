# Scroll Fade

Issue: #91. Original utility for arbitrary scroll containers: `useScrollFade` tracks physical top, bottom, left and right edges on the vertical, horizontal or both axes, follows resize, mutation, load and font readiness, supports RTL, enable/disable and cleanup. `ScrollFade` renders the aria-hidden, pointer-transparent overlay with configurable depth and color. ScrollArea keeps its boolean `fade` prop on top of the same hook; the permanent homepage `a-edge-fade` stays a separate presentation choice. Verify both axes, no-overflow, RTL, reduced motion, forced colors, clean installation and the full responsive matrix. All quality gates remain pending until evidence is recorded.

Aretusa-Item: scroll-fade
