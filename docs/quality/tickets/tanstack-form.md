# TanStack Form

Issue: #89. Typed `TanStackFormField` adapter for fields created with `form.Field` or `useField`, with Aretusa label, help text, error and focus anatomy, `focusFirstInvalidField` for invalid submit and `formatTanStackError` for validator results. Verify text, select, checkbox, radio and switch mapping, synchronous and debounced asynchronous validation, linked errors, focus on invalid submit, pending, success, failure and retry, reset with defaults and array fields, plus the responsive matrix on the documented example. All quality gates remain pending until evidence is recorded.

Aretusa-Item: tanstack-form
